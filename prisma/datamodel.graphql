type User {
  id: ID! @unique
  team: String!
  email: String! @unique
  password: String!
}

type Player {
  id: ID! @unique
  createdAt: DateTime!
  name: String!
  team: String!
  position: String!
  closingtime: Int!
  price: [Float!]
  bidder: [String!]
  bidtimestamp: [DateTime!]
  maxbid: [Float!]
  maxbidder: [String!]
  watchlist: [User!]!
}
